FROM python:3.11-slim

WORKDIR /app
COPY requirements.txt .
RUN pip install -r requirements.txt

COPY . .

# Change "main:app" to match your app (e.g. app:app for Flask)
CMD ["gunicorn", "app:app", "--bind", "0.0.0.0:8080"]